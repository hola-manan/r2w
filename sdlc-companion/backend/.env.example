# Copy to .env and fill in. The backend reads these via app/config.py.
# Vertex AI Express mode API key (https://console.cloud.google.com/vertex-ai).
GOOGLE_API_KEY=...
GOOGLE_MODEL=gemini-2.5-flash
DB_PATH=./data/project.db
CORS_ORIGINS=http://localhost:5173
REQUEST_TIMEOUT=60
